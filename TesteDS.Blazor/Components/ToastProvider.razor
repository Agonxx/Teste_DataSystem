@using Radzen
@using Radzen.Blazor
@using TesteDS.Blazor.Utils

@inject NotificationService NotificationService
@inject ToastBase Toast

<RadzenNotification />

@code {
    protected override void OnInitialized()
    {
        Toast.Init(NotificationService);
    }
}
